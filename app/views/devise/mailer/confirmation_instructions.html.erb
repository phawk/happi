<p>Hey <%= @resource.first_name %>!</p>

<p>👇 Confirm your email using this link</p>

<p><%= link_to "Verify my email", confirmation_url(@resource, confirmation_token: @token), class: "f-fallback button button--primary" %></p>

<p><em>You can’t send outbound messages until your account is confirmed.</em></p>

<%= render "shared/mailer_happi_sig" %>
