<% meta title: t(".heading") %>

<%= render partial: "customers/top_nav", locals: { heading: "All customers" } %>

<%= render partial: "customers/link", collection: @customers, as: :customer, locals: { class: "mb-4" } %>

<% if @customers.empty? %>
  <%= render(EmptyMessageComponent.new(
    title: "You don’t have any customers yet"
  )) do %>
    Follow our <%= link_to "Getting started guide", documentation_root_path, class: "text-purple-500 underline", target: :_blank %> to set up your inbound mailbox or web widget.
  <% end %>
<% end %>
