<%= render Messages::FormComponent.new(
  message_thread: thread,
  message: message,
  current_user: current_user,
  current_team: current_team,
  canned_responses: canned_responses,
  ai_status: local_assigns.fetch(:ai_status, nil)
) %>
